import Layout from '@/components/Layout';
import usePageTitle from '@/hooks/usePageTitle';

const Home = () => {
  usePageTitle('Find Game Friends');

  return <Layout>Home</Layout>;
};

export default Home;
